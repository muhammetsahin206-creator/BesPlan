import React, { useState } from "react";
import {
  View,
  Text,
  TouchableOpacity,
  SafeAreaView,
  StyleSheet,
} from "react-native";

export default function App() {
  const [screen, setScreen] = useState<"home" | "besplan" | "solartenis">("home");

  // Ana menü
  if (screen === "home") {
    return (
      <SafeAreaView style={styles.container}>
        <Text style={styles.title}>Hoş geldin 👋</Text>
        <TouchableOpacity style={styles.btn} onPress={() => setScreen("besplan")}>
          <Text style={styles.btnText}>BesPlan’ı Aç</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.btn} onPress={() => setScreen("solartenis")}>
          <Text style={styles.btnText}>SolarTenis’i Aç</Text>
        </TouchableOpacity>
      </SafeAreaView>
    );
  }

  // BesPlan sahnesi (şimdilik örnek)
  if (screen === "besplan") {
    return (
      <SafeAreaView style={styles.container}>
        <Text style={styles.title}>BesPlan (Test)</Text>
        <Text style={styles.text}>Buraya BesPlan ekranlarını ekleyeceğiz.</Text>
        <TouchableOpacity style={styles.btn} onPress={() => setScreen("home")}>
          <Text style={styles.btnText}>Geri</Text>
        </TouchableOpacity>
      </SafeAreaView>
    );
  }

  // SolarTenis sahnesi (şimdilik örnek)
  if (screen === "solartenis") {
    return (
      <SafeAreaView style={styles.container}>
        <Text style={styles.title}>SolarTenis (Test)</Text>
        <Text style={styles.text}>Buraya SolarTenis ekranlarını ekleyeceğiz.</Text>
        <TouchableOpacity style={styles.btn} onPress={() => setScreen("home")}>
          <Text style={styles.btnText}>Geri</Text>
        </TouchableOpacity>
      </SafeAreaView>
    );
  }

  return null;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#ecf0f1",
    padding: 24,
    justifyContent: "center",
    alignItems: "center",
    gap: 16,
  },
  title: {
    fontSize: 24,
    fontWeight: "700",
    marginBottom: 12,
  },
  text: {
    fontSize: 16,
    textAlign: "center",
  },
  btn: {
    backgroundColor: "#4f46e5",
    paddingVertical: 12,
    paddingHorizontal: 18,
    borderRadius: 10,
    minWidth: 220,
    alignItems: "center",
  },
  btnText: {
    color: "#fff",
    fontWeight: "600",
    fontSize: 16,
  },
});
